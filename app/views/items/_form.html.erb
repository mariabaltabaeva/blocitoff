<%= form_for @item do |f| %>
<div class="form-group">
  <%= f.label :name %>
  <%= f.label :name, class: 'form-control', placeholder: "Enter item name"%>
</div>
<div class="form-group">
  <%= f.submit "Save", class: 'btn btn-success' %>
</div>
<% end %>
