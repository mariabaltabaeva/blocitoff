
<%= link_to "", [item.user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %>
<%= item.name %>
<%= distance_of_time_in_words(item.created_at.strftime("%Y-%m-%d"), Time.now.strftime("%Y-%m-%d"))%>
